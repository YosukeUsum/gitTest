# Tetris CLI — 仕様書 (Spec-Driven Development)

本ドキュメントは仕様駆動開発(Spec-Driven Development)のマスタードキュメントである。
実装・テストはすべて本書の内容に基づいて行い、仕様に変更が必要な場合は
先に本書を更新してから実装を修正する。

## 1. 概要

Python標準ライブラリ(curses)のみで動作する、ターミナル上で遊べるテトリスクローンの
CLIアプリケーション。

## 2. 目的・スコープ

- ターミナル(コンソール)のみで完結する1人用テトリス
- 外部ライブラリへの依存を可能な限り排除する(Windowsのみ `windows-curses` を追加依存として許可)
- ロジック層(盤面・テトリミノ・スコア等)とUI層(curses描画)を分離し、
  ロジック層は自動テスト可能にする

## 3. 用語定義

| 用語 | 意味 |
|---|---|
| ミノ / テトリミノ | 4マスで構成されるブロック(I, O, T, S, Z, J, L の7種) |
| 盤面 (Board) | 幅10・高さ20のグリッド |
| ソフトドロップ | 通常より速く1マスずつ落下させる操作 |
| ハードドロップ | 即座に着地位置まで落下させる操作 |
| ロック | ミノが着地し盤面に固定されること |
| ライン消去 | 横一列がすべて埋まった行を消去すること |
| 7-bagランダム | 7種のミノを1つずつ含む袋からランダムに引く出現方式(偏りを防ぐ) |

## 4. 機能要件 (Functional Requirements)

- FR-1: 盤面は幅10マス、高さ20マスとする。
- FR-2: テトリミノは I/O/T/S/Z/J/L の7種とし、7-bag方式で出現順を決定する。
- FR-3: プレイヤーはミノの左右移動、時計回り/反時計回り回転、
  ソフトドロップ、ハードドロップを行える。
- FR-4: 盤面の壁・床・既に固定されたブロックとの衝突を検出し、
  衝突する移動・回転は無効化する。
- FR-5: 回転により壁際で衝突する場合、簡易ウォールキック
  (左右±1、±2マスのオフセットを試す)を行い、それでも不可なら回転しない。
- FR-6: 横一列が完全に埋まった行はすべて消去し、上のブロックを詰める。
  1回の固定で同時に1〜4行を消去できる。
- FR-7: スコアはライン消去数とレベルに応じて加算する。
  同時消去数が多いほど1行あたりの得点効率が上がる
  (例: 1行=100×level, 2行=300×level, 3行=500×level, 4行(テトリス)=800×level)。
- FR-8: ハードドロップは落下したマス数に応じたボーナス点を加算する
  (1マスあたり2点)。ソフトドロップは1マスあたり1点を加算する。
- FR-9: 消去した累計ライン数が10の倍数に達するごとにレベルが1上昇し、
  落下間隔(重力)が短くなる。
- FR-10: 次に出現するミノを常に1つ先読みしてプレビュー表示する。
- FR-11: 新規ミノの出現位置(盤面上部中央)が既存ブロックと衝突する場合、
  ゲームオーバーとする。
- FR-12: プレイヤーはゲームを一時停止・再開できる。
- FR-13: プレイヤーはいつでもゲームを終了できる。
- FR-14: 盤面に表示するブロック(落下中のミノ・固定済みブロック)は、テトリミノの種類(I/O/T/S/Z/J/L)ごとに色分けして表示すること。色の割り当てはI=シアン、O=黄、T=マゼンタ、S=緑、Z=赤、J=青、L=白(標準8色にオレンジが無いための代替)とする。
- FR-15: 盤面のブロック(落下中のミノ・固定済みブロック)は、輪郭表示ではなく背景色で塗りつぶした「ソリッド表示」で描画すること。あわせて盤面の枠線(`|` `+--+`)、サイドバーのタイトル、「一時停止中」「ゲームオーバー」の状態メッセージに、標準8色の範囲でアクセントカラーを付け、画面全体の配色をより視覚的でモダンな印象にすること。
- FR-16: 横一列が完成した際、即座に消去せず、対象の行を一定時間(点滅などの視覚エフェクト)表示してから消去すること。エフェクト表示中は新しいミノの出現・重力による自然落下・プレイヤーの移動/回転/ドロップ操作を一時停止すること。

## 5. 非機能要件 (Non-Functional Requirements)

- NFR-1: 実行に外部パッケージを必須としない(標準ライブラリの `curses` のみ使用。
  Windows環境でのみ `windows-curses` を利用可能とする)。
- NFR-2: 入力から画面反映までの体感遅延がないこと(1フレーム以内の処理)。
- NFR-3: ロジック層(`board.py`, `tetromino.py`, `game.py`)は `curses` に非依存とし、
  `pytest` により自動テスト可能な設計とする。
- NFR-4: Python 3.9以上で動作すること。
- NFR-5: `ui_curses.py` が画面へ描画する文字列(操作ヘルプ、スコア等のラベル、状態メッセージ)は日本語で表示すること。ただし windows-curses 環境では全角文字を含む文字列を `stdscr.addstr` に渡すと表示幅の計算が不正確になり、文字がずれて重なって表示される不具合がある(2026-08-29 バグ修正、CHANGELOG.md参照)。このため `ui_curses.py` は各文字の表示幅を `unicodedata.east_asian_width` で判定(全角=2桁、半角=1桁)し、確定した桁位置へ1文字ずつ描画すること。盤面の罫線(`|` `+` `-`)・セル表現(`[]`)などレイアウトの基準となる部分は従来どおり ASCII で構成する。(当初は不具合回避のため描画文字列を ASCII 限定としたが、日本語表示の要望を受け、表示幅を明示計算する方針へ改訂。)
- NFR-6: `pytest` を実行するたびに、成功・失敗にかかわらず実行結果を `logs/` ディレクトリへ1ファイルとして記録すること。ログファイル名は実行日時を含む一意な名前(`test_run_<日時>.log`)とし、既存ログを上書きしない(実行1回につき1ファイル)。ログには各テストの成否(PASS/FAIL/ERROR/SKIP)、失敗時の詳細、および合計件数・終了ステータスのサマリを含める。
- NFR-8: ゲームループ(`ui_curses.py` の `run()`)は「入力処理 → 固定タイムステップでの更新 → 描画 → フレームレート制御」の順に明確に分離すること。ゲーム進行の更新(重力によるtickなど)は、実行環境の処理速度やフレームレートのばらつきに関わらず一定間隔で保証すること(固定タイムステップ+アキュムレータ方式)。処理落ちやデバッガでの一時停止等により1フレームの経過時間が極端に大きくなった場合でも、更新処理が無限に追いつこうとして固まる「スパイラル・オブ・デス」を起こさないよう、1回に蓄積できる経過時間に上限を設けること。このタイムステップ制御ロジックはcursesに非依存とし、pytestで単体テスト可能にすること。
- NFR-9: FR-14の色分け表示は、端末が色表示に対応していない場合(`curses.has_colors()` が `False`)は色の初期化をスキップし、従来通りモノクロで描画すること(NFR-1のクラッシュしない要件を維持する)。
- NFR-10: FR-15による塗りつぶし表示・枠線/見出しへの配色は、既存のNFR-9(色非対応端末でのフォールバック)の方針を踏襲し、`curses.has_colors()` が`False` の端末では、ブロックは従来通り輪郭(`[]`)表示、枠線・タイトル・状態メッセージは無配色のままとし、クラッシュしないこと。標準8色は既にI/O/T/S/Z/J/Lの7種で使い切っているため、盤面の空セル自体に新しい背景色を割り当てることはしない(いずれかのミノと同色になり視認性が下がるため)。
- NFR-11: FR-16のエフェクトの時間管理(表示時間・点滅間隔)は `curses` に非依存なロジックとして `Game` に実装し、`pytest` で単体テスト可能にすること(NFR-3の方針を踏襲)。点滅の表現には `curses.A_REVERSE`(反転表示)を用いることで、標準8色を使い切っている状況でも新しい色を追加せず、色対応・非対応端末のどちらでも同じ仕組みでエフェクトを視認できるようにすること。
- NFR-7: 本 `SPEC.md` の機能要件(FR)・非機能要件(NFR)・受け入れ基準(AC)・方針を追加/変更/削除する際は、変更に先立って設計判断の壁打ち(論点・選択肢・トレードオフ・根拠・決定・保留事項)を行い、その**要旨**を `docs/spec-log/` へMarkdownでGit管理下に記録すること。1テーマ1ファイルとし、ファイル名は起票日を含む `YYYY-MM-DD-<topic-slug>.md` とする(議論が後日に及ぶ場合は同一ファイルへ追記し、新規作成しない)。記録対象は `SPEC.md` の要件・方針に関わる壁打ちのみで、通常の質問応答・ツール操作補助・コードのみの作業・チャットの逐語全文は記録しない。誤字修正・体裁整形のみの変更は対象外とする。全ログは `docs/spec-log/README.md` に一覧化する。運用手順の詳細は `CLAUDE.md`、雛形は `docs/spec-log/TEMPLATE.md` を参照。
- NFR-12: `SPEC.md` の機能要件(FR)・非機能要件(NFR)・受け入れ基準(AC)を追加/変更/削除する際は、NFR-7の壁打ちログ運用に加えて、`docs/design/` 配下の説明資料(概要資料pptx・設計書docx・要件一覧xlsx)についても当該変更を反映するよう作成・更新すること。個別の生成・更新手順はCowork上のスキル「design-doc-sync」を参照する。

## 6. アーキテクチャ / モジュール設計

```
tetris-cli/
├── SPEC.md                 本仕様書
├── README.md
├── CHANGELOG.md             開発履歴
├── requirements.txt
├── CLAUDE.md                 Claude Code向け作業ルール(壁打ちログ運用等)
├── conftest.py               pytest実行ログをlogs/へ出力する(NFR-6)
├── logs/                       pytest実行ログの出力先(生成物、.log自体はコミット対象外)
├── docs/
│   ├── spec-log/              SPEC.md変更時の壁打ちログ(NFR-7、Git管理下)
│   │   ├── README.md          壁打ちログ索引
│   │   └── TEMPLATE.md        壁打ちログ雛形
│   └── design/                  説明資料(概要資料pptx・設計書docx・要件一覧xlsx、NFR-12、Git管理下)
├── src/tetris_cli/
│   ├── __init__.py
│   ├── tetromino.py        テトリミノ形状・回転定義(SRS簡易版)
│   ├── board.py             盤面状態・衝突判定・ライン消去
│   ├── game.py               ゲームロジック本体・スコア/レベル管理(UI非依存)
│   ├── game_loop.py         固定タイムステップ制御・フレームレート制御(UI非依存)
│   └── ui_curses.py         curses描画・キー入力(game/game_loopを利用)
├── main.py                     エントリーポイント
└── tests/
    ├── test_tetromino.py
    ├── test_board.py
    ├── test_game.py
    ├── test_game_loop.py
    ├── test_ui_curses_colors.py
    ├── test_line_clear_effect.py
    └── test_line_clear_effect_rendering.py
```

設計方針: `game.py` 以下のロジック層は `curses` を一切importしない。
UIは `ui_curses.py` に閉じ込め、`game.py` の公開メソッドを呼び出すだけにする。
これにより `tests/` からロジックのみを直接テストできる。

## 7. データモデル

- `Board`: `rows=20, cols=10` のグリッド。各セルは `0`(空)または
  ミノ種別を表す非0の整数値(固定ブロックの色情報を兼ねる)。
- `Piece`: `kind`(I/O/T/S/Z/J/L)、`rotation`(0-3)、`x, y`(基準位置)を持つ。
- `GameState`: 現在の `Piece`、次の `Piece`、`Board`、`score`、`level`、
  `lines_cleared`、`game_over`、`paused` を持つ。

## 8. 操作仕様(キーバインド)

| キー | 動作 |
|---|---|
| ← / → | 左右移動 |
| ↓ | ソフトドロップ |
| ↑ または X | 時計回り回転 |
| Z | 反時計回り回転 |
| Space | ハードドロップ |
| P | 一時停止 / 再開 |
| Q | 終了 |

## 9. 受け入れ基準 (Acceptance Criteria)

以下は `tests/` で自動検証する受け入れ基準である。

- AC-1: 新規ミノは盤面上部中央付近の初期位置に出現する。
- AC-2: ミノは盤面の壁・床・既存ブロックを超えて移動できない。
- AC-3: 横一列が完全に埋まると、その行は消去されスコアが加算される。
- AC-4: 同時に4行(テトリス)を消去した場合、1行消去より1行あたりの得点効率が高い。
- AC-5: 累計消去ライン数が10に達するとレベルが2になり、
  レベルに応じて重力(落下間隔)が短くなる。
- AC-6: 新規ミノの出現位置に既にブロックがある場合、`game_over` が `True` になる。
- AC-7: ハードドロップは移動距離に比例したボーナス点を加算し、
  ミノを即座に固定する。
- AC-8: 回転が通常位置で不可能でも、ウォールキックにより
  回転可能になるケースが存在する。
- AC-9: 7-bag方式では、連続する7回のミノ出現に同じ種類の重複がない。
- AC-10: 固定タイムステップのアキュムレータは、蓄積された経過時間に応じて正しい回数の更新ステップ数を返す(実行環境のフレームレートに依存しない)。
- AC-11: 極端に大きな経過時間(処理落ち等を想定)が与えられても、アキュムレータは上限(MAX_FRAME_TIME)でクランプされ、無限に更新し続けることはない。
- AC-12: 色対応端末では、盤面のブロックが背景色で塗りつぶされ、盤面の枠線・タイトル・一時停止/ゲームオーバーのメッセージに色が付いて表示される。色非対応端末では、ブロックは輪郭(`[]`)表示、枠線・見出し・状態メッセージは無配色のまま、従来通りクラッシュせず動作する。
- AC-13: 一列以上が完成すると、`Game.clearing_rows` に消去対象の行番号が設定され、盤面からは即座に消去されない。`Game.update()` を呼び続けて一定時間が経過すると、対象行が盤面から消去され上のブロックが詰められたうえで次のミノが出現する。エフェクト表示中に `Game.tick()` を呼んでも重力による落下は進行せず、移動・回転・ドロップの操作も反映されない。

## 10. スコープ外(将来拡張候補)

- ネットワーク対戦・オンラインランキング
- ホールド機能
- ゴーストピース(落下予測位置の表示)
- SRS完全準拠のウォールキックテーブル(本バージョンは簡易実装)

## 11. 開発プロセス(Spec-Driven Development)

1. 本 `SPEC.md` を作成・確定する(本ステップ)。
2. 受け入れ基準(9章)に対応するテストを `tests/` に作成する。
3. テストが通るようにロジック層(`tetromino.py`, `board.py`, `game.py`)を実装する。
4. `ui_curses.py` / `main.py` でCLI上に統合する。
5. `pytest` を実行し、全受け入れ基準に対応するテストが合格することを確認する。
6. 仕様変更が必要になった場合は、まず設計判断の壁打ちを行って `docs/spec-log/` に
   その要旨を記録し(NFR-7)、次に本 `SPEC.md` を更新してから実装を修正する。
   あわせて `docs/design/` 配下の説明資料(概要資料・設計書・要件一覧)も変更内容を
   反映して更新する(NFR-12)。

## 12. 改訂履歴

| 日付 | 内容 |
|---|---|
| 2026-08-29 | 初版作成。FR-1〜13, NFR-1〜4, AC-1〜9 を定義。 |
| 2026-08-29 | NFR-5を追加。windows-curses環境でCLI操作ヘルプ等の全角文字がずれて重なって表示される不具合が発見されたため、curses描画文字列をASCII限定とする方針を明文化し、`ui_curses.py` を修正(詳細は `CHANGELOG.md` を参照)。 |
| 2026-08-29 | NFR-6を追加。pytest実行のたびに `logs/` へ1ファイルの実行ログを残す要件を明文化し、`conftest.py` を追加して実装(詳細は `CHANGELOG.md` を参照)。 |
| 2026-08-29 | NFR-8・AC-10・AC-11を追加。ゲームループを「入力→固定タイムステップ更新→描画→フレームレート制御」に明確に分離するため、`game_loop.py`(`FixedTimestepLoop`/`FrameLimiter`)を新規実装し、`ui_curses.py` の `run()` を書き換えた。壁打ちログは `docs/spec-log/2026-08-29-game-loop-engineering.md`(詳細は `CHANGELOG.md` も参照)。 |
| 2026-09-12 | FR-14・NFR-9を追加。盤面のブロック(落下中ピース・固定ブロック)をミノの種類ごとに色分け表示するようにした。色非対応端末ではモノクロにフォールバックする。壁打ちログは `docs/spec-log/2026-09-12-block-color-coding.md`(詳細は `CHANGELOG.md` も参照)。 |
| 2026-08-29 | NFR-7を追加。SPEC.md変更時に設計判断の壁打ち要旨を `docs/spec-log/` へ残す運用を明文化。`CLAUDE.md`、`docs/spec-log/`(README/TEMPLATE)を新設。本項の壁打ちログは `docs/spec-log/2026-08-29-spec-brainstorm-logging.md`。 |
| 2026-08-29 | NFR-5を改訂。CLI画面の表示文字列を ASCII 限定から日本語表示へ変更。windows-curses の全角文字重なり不具合は、表示幅を `unicodedata.east_asian_width` で明示計算して1文字ずつ描画する方式で回避する。`ui_curses.py` に `_draw_text()` を追加して実装(詳細は `CHANGELOG.md`、壁打ちログは `docs/spec-log/2026-08-29-cli-japanese-display.md`)。 |
| 2026-09-12 | FR-15・NFR-10・AC-12を追加。盤面のブロックを輪郭表示から塗りつぶし表示に変更し、盤面の枠線・サイドバーのタイトル・一時停止/ゲームオーバーの状態メッセージに標準8色でアクセントカラーを付けた(配色・見た目のモダン化)。色非対応端末では従来通りの無配色表示にフォールバックする。壁打ちログは `docs/spec-log/2026-09-12-modern-color-scheme.md`(詳細は `CHANGELOG.md` も参照)。 |
| 2026-09-12 | FR-16・NFR-11・AC-13を追加。横一列が完成してから実際に盤面から消去されるまでの間、対象行を一定時間・点滅(反転表示)させるライン消去エフェクトを追加した。エフェクト表示中は重力・新規ミノ出現・プレイヤー操作を一時停止する。壁打ちログは `docs/spec-log/2026-09-12-line-clear-effect.md`(詳細は `CHANGELOG.md` も参照)。 |
| 2026-09-12 | NFR-12を追加。SPEC.mdのFR/NFR/AC変更時に、`docs/design/` 配下の説明資料(概要資料pptx・設計書docx・要件一覧xlsx)も更新することを明文化した。6章のアーキテクチャ図・11章の開発プロセスにも反映した。壁打ちログは `docs/spec-log/2026-09-12-design-doc-sync.md`(詳細は `CHANGELOG.md` も参照)。 |
