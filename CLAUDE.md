# CLAUDE.md — Tetris CLI 作業ルール

このリポジトリは仕様駆動開発(Spec-Driven Development)で進める。`SPEC.md` がマスタードキュメントであり、
実装・テストは常に `SPEC.md` に従う。仕様変更が必要なときは、先に `SPEC.md` を更新してから実装を直す。

## SPEC.md を変更するときの手順(壁打ちログ / NFR-7)

`SPEC.md` の機能要件(FR)・非機能要件(NFR)・受け入れ基準(AC)・方針を追加/変更/削除する場合は、
以下に従うこと。誤字修正・体裁整形のみの変更は対象外。

1. **壁打ちする**: いきなり `SPEC.md` を編集しない。まずユーザーと論点・選択肢・トレードオフを
   やり取りして設計判断を固める。
2. **壁打ちログを起票する**: 議論が要件・方針に及んだ時点で
   `docs/spec-log/YYYY-MM-DD-<topic-slug>.md` を作成し、
   「壁打ちログを開始します: <パス>」とユーザーに告げる。
   雛形は `docs/spec-log/TEMPLATE.md` を使う。
3. **議論の要旨を追記する**: チャットの逐語全文は残さない。
   「何を検討し、なぜそう決めたか」(論点・比較した選択肢・ユーザーの回答・決定事項・保留)を書く。
4. **索引を更新する**: 起票・更新のたびに `docs/spec-log/README.md` の一覧へ反映する。
5. **SPEC.md を更新する**: 決定に基づき `SPEC.md` の該当節、`12. 改訂履歴`、必要なら
   `6. モジュール設計` のツリーを更新する。
6. **説明資料を更新する(NFR-12)**: `docs/design/` 配下の概要資料(pptx)・設計書(docx)・
   要件一覧(xlsx)を、SPEC.mdの変更内容に合わせて作成・更新する。生成・更新手順はCowork上の
   スキル「design-doc-sync」を参照する。
7. **ログを締める**: 反映した節・要件ID・要約・コミットハッシュ(分かれば)をログに記録し、
   ステータスを `決定済み` または `見送り` に更新する。

### 記録する / しない

- 記録する: `SPEC.md` の要件・方針に関わる壁打ちだけ。
- 記録しない: 通常の質問応答、ツールや環境の操作補助、コードのみの作業、雑談。

### 保存先の扱い

- `docs/spec-log/` は **Git 管理下**(コミット対象)。設計判断の根拠は資産として残す。
- 生成物である `logs/`(pytest 実行ログ, NFR-6)とは別物。混同しない。
- 1テーマ = 1ファイル。議論が後日のセッションに続く場合は同じファイルに
  `## セッション: YYYY-MM-DD` 見出しを足して追記し、新規ファイルを作らない。

## 役割定義(PM / PL / BA / QA)

一人のエンジニアでも、要望を「要件化」「設計」「実装」「検証」の観点に分けて扱えるよう、
`.claude/agents/` に4つの役割を定義している。Claude Codeのサブエージェント機能
(`Task`ツールで `subagent_type: pm | pl | ba | qa` を指定)から呼び出せる。

- **PM**(`.claude/agents/pm.md`): 要望のスコープ確定・優先順位判断・進捗報告。
  実装や `SPEC.md` の直接編集はしない。
- **PL**(`.claude/agents/pl.md`): アーキテクチャ判断・実装方式の比較検討・
  壁打ちログの下書き作成。
- **BA**(`.claude/agents/ba.md`): 曖昧な要望をFR/NFR/ACへ構造化し、壁打ちログを起票する。
- **QA**(`.claude/agents/qa.md`): 受け入れ基準(AC)の検証・テスト実行・`logs/` の確認。
- **評価者**(`.claude/agents/reviewer.md`): 実装・テストの作成が完了した直後に、
  選定したテスト観点(正常系・異常系・境界値・NFRのフォールバック等)が要件(FR/NFR/AC)に
  対して過不足なく妥当かを、独立した視点でレビューする。テストの作成・実行・修正はしない。

通常の(単独エンジニアとしての)作業フローは、これまで通り上記「壁打ちログ」の運用に従う。
役割を使い分けるのは、要望が複雑で「要件・設計・検証」の観点を明確に分離したいとき、
あるいは実装内容を別の視点でレビューさせたいときを想定している。
各ロールの詳細な責務・参照ドキュメント・出力形式は、それぞれの `.claude/agents/*.md` を参照。

### 実装・テスト完了後の観点レビュー(評価者、NFR-13)

新機能の実装・バグ修正など、`tests/` にテストを新規作成または変更した場合は、
`pytest`(Windows環境では `py -3 -m pytest`)による通常のテスト実行で全件合格することを
確認した**後**に、評価者(`.claude/agents/reviewer.md`)によるテスト観点レビューを行う。
誤字修正・体裁整形のみの変更(壁打ちログの対象外となる軽微な変更)には適用しない。

評価者は、実装を行った作業セッションの中で `Task` ツール(`subagent_type: reviewer`)を
使って呼び出すのではなく、**別セッション**として独立に起動する(例: 新しいターミナルで
`claude --agent reviewer` を実行する、または `claude --bg` でバックグラウンド起動する)。
別セッションは実装セッションの会話履歴を引き継がないため、呼び出す際はレビュー対象
(該当する要件ID、対象のテストファイルパス)を明示的に伝えること。評価者から指摘が
あれば、実装セッションに戻ってテストを追加・修正し、`pytest` を再実行して合格を確認する。

## テスト実行

- このリポジトリのコードは Python 3.9 以上前提(NFR-4)。`from __future__ import annotations` を使うため
  Python 3.6 では `conftest.py` の読み込みに失敗する。
- Windows では `python` が 3.6 を指すことがある。`py -3`(3.8+)で
  `py -3 -m pytest` を実行する。`pytest` 未導入なら `py -3 -m pip install "pytest>=7.0"`。
- `pytest` 実行のたびに `logs/test_run_<日時>.log` が 1 ファイル生成される(NFR-6、`conftest.py` が実装)。

## curses 描画の制約(NFR-5)

`src/tetris_cli/ui_curses.py` の画面表示文字列は日本語を使ってよい。ただし windows-curses は全角文字を含む文字列を `stdscr.addstr` にそのまま渡すとカーソルが1桁しか進まず、全角グリフの右半分に次の文字が重なって表示される不具合があるため、日本語を含むテキストは必ず `_draw_text()`(`unicodedata.east_asian_width` で表示幅を判定し1文字ずつ確定桁へ描画)経由で描画すること。`stdscr.addstr` を直接使ってよいのは、盤面の罫線・セル表現などASCIIのみで構成される部分に限る。
